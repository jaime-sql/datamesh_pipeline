# Copy this file to `.env` and fill in real values.
# `.env` is git-ignored and will never be committed.
#
# Only fill in the ones you actually have. Leave the other blank/unset.

# Neon connection string (Project dashboard -> Connection Details)
NEON_DATABASE_URL=

# Supabase connection string (Project Settings -> Database -> Connection string -> URI)
SUPABASE_DATABASE_URL=
